.forecasts-container
  .row
    .group-standings-panel
      .title
        = 'Points to assign'
      %table
        %tr.header
          %th.name Participant
          %th Points
          - @points.each do |user|
            %tr
              %td.name
                = user.name.present? ? user.name : user.email
              %td= user.points_to_add || 0

  .row
    = form_tag '/puntos', method: 'post' do
      = submit_tag 'Assign points', class: ['button', 'save-forecast-btn']