.row
  .playoffs
    -# OCTAVOS A
    .columns.stage.left-side
      .four
      .four
      .four
      .four

    -# CUARTOS A
    .columns.stage.left-side
      .two
        = render partial: 'playoff_match_disabled', object: @matches.where(stage: 8, pos_in_stage: 1).first
      .two
        = render partial: 'playoff_match_disabled', object: @matches.where(stage: 8, pos_in_stage: 2).first

    -# SEMIS A
    .columns.stage.left-side
      .one
        = render partial: 'playoff_match_disabled', object: @matches.where(stage: 4, pos_in_stage: 1).first

    -# FINAL
    .columns.stage.center-side
      .three
        = render partial: 'playoff_match_disabled', object: @matches.where(stage: 2, pos_in_stage: 1).first
      .three
        = render partial: 'playoff_match_disabled', object: @matches.where(stage: 2, pos_in_stage: 2).first

    -# SEMIS B
    .columns.stage.right-side
      .one
        = render partial: 'playoff_match_disabled', object: @matches.where(stage: 4, pos_in_stage: 2).first

    -# CUARTOS B
    .columns.stage.right-side
      .two
        = render partial: 'playoff_match_disabled', object: @matches.where(stage: 8, pos_in_stage: 3).first
      .two
        = render partial: 'playoff_match_disabled', object: @matches.where(stage: 8, pos_in_stage: 4).first

    -# OCTAVOS B
    .columns.stage.right-side
      .four
      .four
      .four
      .four