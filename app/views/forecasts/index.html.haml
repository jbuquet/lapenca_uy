.forecasts-container
  .row
    .small-8.columns.groups
      = form_tag do
        - @matches.each do |group, matches|
          .group-panel
            .title
              Grupo #{group}
            .matches
              .content{id: "panel#{group}"}
                .row
                  .small-7.columns
                    = render partial: 'match_forecast', collection: matches, as: :match_forecast
                  .small-5.columns
                    - teams = Team.where(group: group)
                    .positions
                      %table{data: {teams: teams.map { |t| t.name.downcase }}}
                        %tr.header
                          %th.team Equipo
                          %th pts
                          %th dif
                        - teams.each do |team|
                          %tr
                            %td.team= team.name
                            %td.pts
                            %td.dif

        <br />
        = submit_tag 'Actualizar Pronostico', class: ['button', 'save-forecast-btn']

    .small-4.columns.playoffs
      .row.auto-height
        -# OCTAVOS A
        .columns.stage
          .row.eight
            .match
              = render partial: 'playoff_match_forecast'
          .row.eight
            .match
              = render partial: 'playoff_match_forecast'
          .row.eight
            .match
              = render partial: 'playoff_match_forecast'
          .row.eight
            .match
              = render partial: 'playoff_match_forecast'
          .row.eight
            .match
              = render partial: 'playoff_match_forecast'
          .row.eight
            .match
              = render partial: 'playoff_match_forecast'
          .row.eight
            .match
              = render partial: 'playoff_match_forecast'
          .row.eight
            .match
              = render partial: 'playoff_match_forecast'

        -# CUARTOS A
        .columns.stage
          .row.four
            .match
              = render partial: 'playoff_match_forecast'
          .row.four
            .match
              = render partial: 'playoff_match_forecast'
          .row.four
            .match
              = render partial: 'playoff_match_forecast'
          .row.four
            .match
              = render partial: 'playoff_match_forecast'

        -# SEMIS A
        .columns.stage
          .row.two
            .match
              = render partial: 'playoff_match_forecast'
          .row.two
            .match
              = render partial: 'playoff_match_forecast'

        -# FINAL
        .columns.stage
          .row.one
            .match
              = render partial: 'playoff_match_forecast'

        -# SEMIS B
        .columns.stage
          .row.two
            .match
              = render partial: 'playoff_match_forecast'
          .row.two
            .match
              = render partial: 'playoff_match_forecast'

        -# CUARTOS B
        .columns.stage
          .row.four
            .match
              = render partial: 'playoff_match_forecast'
          .row.four
            .match
              = render partial: 'playoff_match_forecast'
          .row.four
            .match
              = render partial: 'playoff_match_forecast'
          .row.four
            .match
              = render partial: 'playoff_match_forecast'

        -# OCTAVOS B
        .columns.stage
          .row.eight
            .match
              = render partial: 'playoff_match_forecast'
          .row.eight
            .match
              = render partial: 'playoff_match_forecast'
          .row.eight
            .match
              = render partial: 'playoff_match_forecast'
          .row.eight
            .match
              = render partial: 'playoff_match_forecast'
          .row.eight
            .match
              = render partial: 'playoff_match_forecast'
          .row.eight
            .match
              = render partial: 'playoff_match_forecast'
          .row.eight
            .match
              = render partial: 'playoff_match_forecast'
          .row.eight
            .match
              = render partial: 'playoff_match_forecast'


:javascript
  forecast()